ext.foldProfile = [
    versionName: "0.119.0-fold.1",
    versionCode: 2026082001,
    applicationLabel: "Termux Fold",
    packageVariant: "apt-android-7",
    supportedAbis: ["arm64-v8a"].asImmutable()
].asImmutable()

buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.13.2"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}
